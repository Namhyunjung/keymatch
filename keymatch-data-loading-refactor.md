# 키맞추기 — pipelineData 인라인 임베드 제거 작업 지시서

## 배경 (문제 진단)

`https://keymatch-steel.vercel.app/` 의 응답 HTML을 직접 받아서 확인한 결과:

- 전체 HTML 문서 크기: **약 25MB (24,983,610자)**
- 그중 `<script type="application/json" id="pipelineData">` 태그 하나가 **24.9MB (전체의 99.8%)**
- 즉 전체 지역 × 전체 단지 × 전체 평형 × 전체 정책 레짐 구간의 원본/가공 데이터를 페이지 최초 로드 시 통째로 HTML에 박아넣고 있음

**부작용**
1. 브라우저 "소스 보기"가 25MB 텍스트를 문법 강조하며 렌더링하려다 무한 대기
2. 최초 페이지 로드 자체가 느림 (특히 모바일)
3. Googlebot/애드센스 심사 크롤러의 렌더링 예산(time/resource budget)을 초과할 가능성 — 현재 애드센스 사이트 상태가 "준비 중"에서 오래 머무는 것과 무관하지 않을 수 있음

**원칙**: 이건 분석 로직(정책 레짐 세그멘테이션, 평형그룹 무결성, thin-trade 처리)의 문제가 아니라 **전달(transport) 방식**의 문제입니다. 계산 결과는 그대로 두고, "얼마나 필요한 만큼만, 언제 내려주느냐"만 바꾸는 작업입니다.

## 목표

- 최초 페이지 HTML에서 `pipelineData` 같은 대용량 인라인 임베드를 완전히 제거
- 화면(대시보드 / 동조단지 / 단지비교)이 실제로 필요로 하는 만큼만, 필요한 시점에 fetch
- 계산된 값(레짐 경계, 평당가, 동조 판정 등)은 절대 바뀌지 않아야 함 — 순수 리팩터링

## 제안 아키텍처

### 1. 데이터 계층
파이프라인(배치)이 이미 단지×평형×레짐 단위로 결과를 계산해서 DB(로컬 SQLite / 운영 Postgres)에 저장하고 있음. 여기서 한 걸음 더 나아가 **지역 단위로 쪼갠 정적 JSON**을 만들어 배포하는 방식을 권장:

```
public/data/regions/{regionId}/{pyeong}/dashboard.json   # 대장단지 + 선택 평형의 레짐별 지수
public/data/regions/{regionId}/{pyeong}/synced.json       # 동조단지 목록 (요약 정보만)
public/data/complexes/{complexId}/{pyeong}/series.json    # 특정 단지의 전체 시계열 (단지비교용)
```

- 정적 파일이라 Vercel Edge/CDN 캐싱이 공짜로 적용됨
- 배치 파이프라인 실행 후 "지역별 JSON 생성" 스텝만 추가하면 됨 (기존 계산 로직 재사용, export 형태만 추가)
- DB를 매 요청마다 붙잡을 필요가 없어서 서버리스 환경(Vercel)에 잘 맞음

(DB를 직접 쿼리하는 API 라우트 방식도 가능하지만, 현재 규모에서는 정적 JSON이 더 단순하고 안정적입니다.)

### 2. API/라우트 계층 (정적 파일을 안 쓰는 경우의 대안)
```
GET /api/regions/[regionId]?pyeong=84       → 대시보드 페이로드
GET /api/regions/[regionId]/synced?pyeong=84 → 동조단지 목록
GET /api/complexes/[complexId]/series?pyeong=84 → 단지비교용 개별 단지 시계열
```

### 3. 클라이언트 계층
- `<script id="pipelineData">` 인라인 임베드 **완전 삭제**
- 첫 화면(01 대시보드)은 현재 선택된 지역×평형의 `dashboard.json`만 fetch
- "지역 변경" 시 새 지역의 `dashboard.json`만 다시 fetch (다른 지역 데이터는 안 건드림)
- 02 동조단지 탭은 **그 탭을 열 때만** `synced.json` fetch (미리 로드 금지)
- 03 단지비교 탭은 사용자가 비교할 단지를 고를 때 **그 단지들의 series.json만** 개별 fetch
- 탭 간 재방문 시 중복 요청 방지를 위해 SWR 또는 React Query 같은 캐싱 레이어 사용 권장 (아직 없다면 도입)

## 완료 기준 (Acceptance Criteria)

- [ ] `/` 최초 응답 HTML 크기가 수백 KB 이하로 감소 (25MB → 대폭 축소)
- [ ] 브라우저 "소스 보기"가 즉시 열림
- [ ] 지역 변경 시 네트워크 탭에서 해당 지역의 작은 JSON만 요청되는지 확인 (다른 지역 데이터 요청 없음)
- [ ] 동조단지/단지비교 탭을 열기 전까지는 관련 데이터가 요청되지 않는지 확인
- [ ] 대시보드에 표시되는 수치(대장단지 지수, 동조단지 9개, 평당가 등)가 리팩터링 전후로 **완전히 동일**한지 회귀 테스트
- [ ] `view-source:` 또는 Chrome DevTools Network 탭에서 문서 응답 크기 확인

## 참고 컨텍스트 (기존 프로젝트 메모)

- 정책 레짐 4구간: 규제강화기(17.05–19.12), 급등기(20.01–22.04), 하락기(22.05–23.12), 회복기(24.01–)
- 분석 단위는 항상 "단지×평형" — 평형그룹 섞어서 비교하면 안 됨
- 월 거래 5건 미만 thin-trade 구간은 별도 처리 필요 (증여성 저가 직거래 방지 로직 있음)
- 이 로직/판정 결과는 이번 작업에서 절대 변경하지 말 것 — 오직 "언제/얼마나 내려주는지"만 변경
