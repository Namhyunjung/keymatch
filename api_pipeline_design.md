# 국토부 API 연동 스펙 & 데이터 파이프라인 설계

## 1. 사용할 API

**국토교통부_아파트매매 실거래자료 (상세)** — 공공데이터포털(data.go.kr)

| 항목 | 내용 |
|---|---|
| 엔드포인트 | `http://apis.data.go.kr/1613000/RTMSDataSvcAptTradeDev/getRTMSDataSvcAptTradeDev` |
| 응답 형식 | XML (JSON 옵션 없음 — 파싱 필요) |
| 인증 | data.go.kr 회원가입 → 활용신청(아파트매매는 자동승인) → 서비스키 발급 |
| 키 종류 | Encoding key / Decoding key 2종류 발급됨. `requests` 라이브러리로 직접 파라미터 조립 시 **Decoding key** 사용 (URL 인코딩은 라이브러리가 처리) |

**요청 파라미터**

| 파라미터 | 설명 | 예시 |
|---|---|---|
| `serviceKey` | 발급받은 인증키 | - |
| `LAWD_CD` | 법정동코드 앞 5자리 (시군구 단위) | `11680` (강남구) |
| `DEAL_YMD` | 계약년월 6자리 | `202608` |
| `pageNo` | 페이지 번호 | `1` |
| `numOfRows` | 페이지당 건수 (최대 권장 100) | `100` |

> **주의**: 이 API는 한 번에 "시군구 1개 × 월 1개"만 조회 가능해요. 여러 구를 조회하려면 법정동코드별로 반복 호출해야 해요. 지번주소 기반 정밀 검색은 없어서, 검색 결과 안에서 아파트명으로 필터링해야 해요.

**응답 주요 필드 → 우리 스키마 매핑**

| API 필드 | 의미 | 매핑 대상 (schema.sql) |
|---|---|---|
| `aptSeq` | 국토부 자체 단지식별자 (`시군구코드-일련번호`) | `complexes.apt_seq` — **1차 매칭키로 사용** |
| `aptNm` | 아파트명 | `complexes.complex_name` (보조 매칭키) |
| `umdNm` / `umdCd` | 법정동명/코드 | `regions` 매칭 |
| `excluUseAr` | 전용면적(㎡) | `transactions.area_m2` → `pyeong_groups` 매핑 |
| `dealAmount` | 거래금액 (문자열, 콤마 포함 예: `"115,000"`) | 콤마 제거 후 `transactions.price_10k` |
| `dealYear`/`dealMonth`/`dealDay` | 계약년/월/일 | `transactions.txn_ym`, `txn_day` |
| `floor` | 층 | `transactions.floor` |
| `buildYear` | 건축년도 | `complexes.built_year` |
| `dealingGbn` | 거래유형(중개/직거래) | `transactions.is_direct_txn` |
| `cdealType` | **해제여부** (`'O'`=해제) | `transactions.is_cancelled` |
| `cdealDay` | 해제사유발생일 | `transactions.cancelled_date` |
| `jibun`, `roadNm` 등 | 지번/도로명 | `complexes.road_address` |

**`aptSeq`를 1차 매칭키로 쓰는 이유**: 국토부 API가 단지별로 안정적인 식별자(`aptSeq`)를 제공해요. 이게 있으면 "아파트명 텍스트로 fuzzy match"보다 훨씬 신뢰도 높게 같은 단지를 추적할 수 있어요 (동명 아파트가 여러 지역에 있어도 혼동 없음). `aptNm` 매칭은 `aptSeq`가 비어있는 예외 케이스에서만 fallback으로 써요.

**해제 거래 처리가 중요한 이유**: 계약 후 취소된 거래(`cdealType='O'`)가 원본 데이터에 남아있어요. 이걸 걸러내지 않으면 실제보다 높거나 낮은 가격이 "최고가"로 잡혀서 상승률 계산이 왜곡돼요. 매일 재수집 배치에서 기존 건의 `cdealType`이 새로 `'O'`로 바뀌었는지 반드시 체크해야 해요.

---

## 2. 운영상 제약사항

- **호출 단위 제한**: 한 번 호출에 시군구 1개 × 월 1개만 가능 → 지역이 많고 기간이 길수록 호출 횟수가 기하급수적으로 늘어남 (예: 서울 25개구 × 108개월 ≈ 2,700회 호출, 페이지네이션 포함하면 더 늘어남)
- **트래픽 한도**: 개발계정은 기본 트래픽 한도가 있고, 운영계정 전환 신청이 필요할 수 있음 (실사용 트래픽 규모를 보고 공공데이터포털에 운영계정 신청)
- **데이터 지연/정정**: 신고 후 실제 등록까지 시차가 있고, 계약 해제도 나중에 반영됨 → **최근 3개월은 매일 재수집**해야 해제·정정 건을 놓치지 않음
- **법정동코드 마스터**: 행정표준코드관리시스템(code.go.kr)의 법정동코드 전체자료를 별도로 받아 `regions` 테이블 시딩에 사용 (API 자체에는 지역 목록 조회 기능 없음)

---

## 3. 파이프라인 아키텍처

```
[법정동코드 마스터 시딩] (최초 1회 + 연 1회 갱신)
        │
        ▼
┌───────────────────────────────────────────────┐
│  Extract: 국토부 API 호출                        │
│  - LAWD_CD × DEAL_YMD 루프                       │
│  - 페이지네이션 (totalCount 기준 반복)              │
│  - 응답 XML 원본을 그대로 raw 저장 (재처리 대비)      │
└───────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────┐
│  Transform                                      │
│  1) 필드 파싱 (콤마 제거, 타입 변환)                 │
│  2) 단지 매칭 (aptSeq 우선 → 없으면 아파트명+법정동)   │
│     → 신규 단지면 complexes에 upsert              │
│  3) 평형 그룹 매핑 (excluUseAr → pyeong_group_id) │
│  4) 해제건 처리 (cdealType='O' → is_cancelled)   │
│  5) 이상치 트리밍 (상하위 5%, 직거래 별도 플래그)      │
└───────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────┐
│  Load                                           │
│  - transactions 테이블 upsert (raw_source_id 기준)│
│  - monthly_price 재계산 (영향받은 complex×ym만)     │
└───────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────┐
│  배치 재계산 (매일 새벽, 변경분만)                    │
│  - regime_returns 재계산                         │
│  - leader_complexes 재판정                       │
│  - sync_results / sync_summary 재계산            │
└───────────────────────────────────────────────┘
```

### 스케줄

| 작업 | 주기 | 범위 |
|---|---|---|
| 최근월 수집 | 매일 새벽 | 최근 3개월 (신고지연·해제 반영) |
| 신규 지역 백필 | 온디맨드 | 사용자가 새 지역 검색 시, 2017.05~현재 전체 수집 후 캐시 |
| 법정동코드 마스터 갱신 | 연 1회 | 행정구역 개편 반영 |
| monthly_price 재계산 | 매일, 변경분만 | 영향받은 complex×ym |
| regime_returns / sync_summary 재계산 | 매일, 변경분만 | 영향받은 complex |
| 전체 재계산 (파라미터 변경 시) | 온디맨드 | α, n 값 조정 후 전체 배치 |

**신규 지역 백필을 온디맨드로 두는 이유**: 전국을 미리 다 수집하면 호출량이 감당 안 돼요. 사용자가 실제로 조회하는 지역만 그때그때 백필하고, 이후엔 일일 배치로 유지하는 방식이 현실적이에요.

---

## 4. 예외 처리 설계

| 상황 | 처리 |
|---|---|
| API 타임아웃/5xx | 지수 백오프 재시도 (최대 3회), 실패 시 실패 큐에 적재 후 다음 배치에서 재시도 |
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 등 인증 에러 | 즉시 알림, 배치 중단 (트래픽 낭비 방지) |
| 응답 건수 0건 | 정상 케이스로 처리 (해당 월 거래 없음) — 빈 레코드로 마킹해 재조회 방지 |
| 동일 거래 중복 수신 | `raw_source_id`(법정동+연월+aptSeq+계약일+거래금액+층 조합 해시) 기준 upsert |
| 단지 매칭 실패 (aptSeq 없고 이름도 모호) | `match_confidence` 낮게 저장 후 관리자 검수 큐로 전송, 자동 매칭 보류 |

---

## 5. 참고: 커넥터 구현체

동일 산출물의 `apt_trade_connector.py`에 위 스펙을 반영한 실행 가능한 파이썬 코드가 있어요. `sync_engine.py`가 기대하는 `monthly_price` 형태(`ym`, `rep_price_10k`, `txn_count`)로 바로 이어지도록 변환 함수까지 포함했어요.
