# 백테스트: α, n 파라미터 검증 방법론

목적은 두 가지예요. **① α(오차범위)와 n(윈도우)이 과적합되지 않았는지 확인**하고, **② "몇 국면이 동조하면 다음 국면도 동조할 확률이 높은지" 검증**하는 거예요.

---

## 1. Walk-forward 검증 (핵심 방법)

미래 데이터를 미리 보지 않도록, 시간 순서대로 한 국면씩 밀면서 검증해요.

```
Step 1: 국면 1로 대장단지 판정 (다음 스텝 이후 고정)
Step 2: 국면 1~2 데이터로 "동조단지" 후보 선정
        → 국면 3에서도 실제로 동조하는지 확인 (out-of-sample)
Step 3: 국면 1~3 데이터로 "동조단지" 후보 선정
        → 국면 4에서도 실제로 동조하는지 확인 (out-of-sample)
```

- 이렇게 하면 매 스텝이 "과거로 미래를 맞히는" 검증이 되고, 미래 데이터를 써서 과거를 재단하는 look-ahead bias를 피할 수 있어요.
- 국면이 4개뿐이라 스텝 수가 적은 게 한계예요 (2번의 out-of-sample 테스트만 가능). 이건 나중에 §4에서 보완해요.

### 평가 지표
| 지표 | 정의 |
|---|---|
| **Precision** | (동조 예측 & 실제로도 동조) / 전체 동조 예측 건수 |
| **Recall** | (동조 예측 & 실제로도 동조) / 전체 실제 동조 건수 |
| **이탈률** | 동조 예측됐다가 다음 국면에 이탈한 단지 비율 (사용자 신뢰도에 직결되는 지표) |

Precision을 가장 중요하게 봐요. "동조단지라고 보여줬는데 다음 국면엔 안 맞았다"가 사용자 신뢰를 가장 크게 깎아먹기 때문이에요.

---

## 2. 파라미터 그리드서치

α와 n을 격자로 바꿔가며 Walk-forward Precision을 비교해요.

```
α ∈ {8%p, 10%p, 12%p, 15%p, 20%p}
n ∈ {3개월, 6개월, 9개월}
```

- 총 15개 조합 × 국면별 walk-forward 실행
- α가 너무 작으면(예: 8%p) 동조단지가 거의 안 남고(recall 급감), 너무 크면(예: 20%p) 아무 단지나 다 동조로 잡혀서 precision이 떨어져요.
- **선택 기준**: precision-recall 커브에서 "무릎(knee) 지점" — precision이 급격히 꺾이기 직전의 가장 관대한 α를 선택해요. (무조건 precision 최댓값을 고르면 리스트가 너무 좁아져서 서비스 가치가 없어짐)

---

## 3. 지역별 최소 표본 조건

후보 단지가 너무 적은 지역(예: 대장단지 포함 5개 미만)은 백테스트에서 제외해요. 통계적으로 의미 있는 검증이 안 되기 때문이에요.

- 백테스트 대상: 후보 단지 20개 이상인 지역만
- 서비스에는 노출하되, 표본이 적은 지역은 UI에 "표본 부족 · 참고용" 배지를 달아 신뢰도 착시를 방지

---

## 4. 국면 수 부족 문제 보완 — 슬라이딩 윈도우 서브국면

국면이 4개뿐이라 walk-forward 스텝이 2번뿐인 한계를 보완하려면, 정책 국면과 별개로 **12개월 롤링 윈도우**로 서브 검증을 추가해요.

```
매 12개월을 하나의 "미니 국면"으로 보고,
t~t+12 데이터로 예측 → t+12~t+24 실제와 비교, 1개월씩 밀며 반복
```

- 이건 정책 국면 로직과는 별개의 **보조 검증**이에요. 실제 서비스 로직(§본문)은 정책 국면 기준을 유지하되, α/n 값의 안정성만 이 방식으로 추가 확인해요.
- 표본 수가 많아져서 그리드서치 결과의 신뢰구간을 좁힐 수 있어요.

---

## 5. 백테스트 결과 리포트 형식

```
지역: 서울 강남구 대치동 | 평형: 84㎡ | 대장단지: 대치 SK뷰
파라미터: α=12%p, n=6개월

┌────────────────┬────────┬────────┬───────────┐
│ 검증 스텝        │ 예측건수 │ 적중건수 │ Precision │
├────────────────┼────────┼────────┼───────────┤
│ 국면1-2 → 국면3   │   14   │   11   │   78.6%   │
│ 국면1-3 → 국면4   │   11   │    9   │   81.8%   │
└────────────────┴────────┴────────┴───────────┘
```

- 이 리포트를 지역별로 쌓아서 전국 평균 precision을 관리 대시보드에서 모니터링해요.
- α 기본값(12%p)은 이 리포트가 쌓이면 실측 데이터로 재조정하고, 초기값은 어디까지나 가설로 시작하는 값이에요.

---

## 6. 실행 순서 요약

```
1. 지역×평형별 후보단지 20개 이상인 지역만 추출
2. 국면 경계마다 walk-forward: (이전국면들로 동조판정) vs (다음국면 실제결과)
3. α×n 그리드서치 → precision-recall 커브 산출
4. 지역별 knee-point α 확인 → 전국 공통값 or 지역별 값 결정
5. 보조로 12개월 롤링 윈도우 검증 → 안정성 재확인
6. 결과를 sync_summary 캐시 재계산 배치에 반영
```
